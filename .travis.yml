language: c
os:
    - linux
compiler:
    - gcc
script:
    - make all
    - make tests
deploy:
    provider: releases
        api_key:
            secure: $(git-token)
        file: main.out
        skip_cleanup: true
    on:
        tags: true
